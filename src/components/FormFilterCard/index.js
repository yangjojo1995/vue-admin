import FormFilterCard from './src/FormFilterCard.vue'

FormFilterCard.install = function (Vue) {
  Vue.component(FormFilterCard.name, FormFilterCard)
}

export { FormFilterCard }
export default FormFilterCard
